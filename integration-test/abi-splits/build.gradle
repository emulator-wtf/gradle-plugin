buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:8.6.0'
  }
}

plugins {
  id "wtf.emulator.gradle"
}

dependencies {
  emulatorwtf(project(":app"))
}
